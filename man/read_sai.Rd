% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/saitem.R
\name{read_sai}
\alias{read_sai}
\title{Read an SA-item}
\usage{
read_sai(jsai)
}
\arguments{
\item{jsai}{Java SA-item object.}
}
\value{
a list
}
\description{
\code{read_sai()} extracts all the information of a SA-item (see details).
}
\details{
A SA-item contains more information than just the results of an estimation.
Full information is extracted with the \code{read_sai()} function that
returns a list of 5 objects:
\itemize{
\item \code{ts}: raw time series.
\item \code{referenceSpec}: initial specification. Reference when refreshing and relaxing constraints.
\item \code{estimationSpec}: specification used for the current estimation.
\item \code{resultSpec}: specification containing all parameters stemming from \code{estimationSpec} (fully identified model).
\item \code{results}: results of the estimation.
}
}
\examples{
\dontshow{if (rjd3jars::check_java_version()) withAutoprint(\{ # examplesIf}

# Load a Workspace
file <- system.file("workspaces", "workspace_test.xml", package = "rjd3workspace")
\donttest{
jws <- jws_open(file)

# Select SAProcessing
jsap1 <- jws_sap(jws, 1)

# Select SA-item (as java object)
jsai1 <- jsap_sai(jsap1, 3)

 # read SA-item
read_sai(jsai = jsai1)
}


\dontshow{\}) # examplesIf}
}
