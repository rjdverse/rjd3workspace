% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiprocessing.R
\name{add_sa_item}
\alias{add_sa_item}
\title{Add SAItem to Multiprocessing}
\usage{
add_sa_item(jmp, name, x, spec, ...)
}
\arguments{
\item{jmp}{the multiprocessing.}

\item{name}{the name of SAItem.}

\item{x}{either a seasonal adjustment model (from \code{\link[rjd3x13:x13]{rjd3x13::x13()}} or \code{\link[rjd3tramoseats:tramoseats]{rjd3tramoseats::tramoseats()}}), a SaItem or a \code{"ts"} object.}

\item{spec}{the specification to use when \code{x} is a \code{"ts"} object.}

\item{...}{other unused parameters.}
}
\description{
Add SAItem to Multiprocessing
}
\examples{
dir <- tempdir()
y <- rjd3toolkit::ABS$X0.2.09.10.M
jws <- .jws_new()
jmp1 <- .jws_multiprocessing_new(jws, "sa1")
add_sa_item(jmp1, name = "x13", x = rjd3x13::x13(y))
add_sa_item(jmp1, name = "tramo", x = rjd3tramoseats::tramoseats(y))
add_sa_item(jmp1, name = "x13-2", x = y, rjd3x13::spec_x13())
add_sa_item(jmp1, name = "tramo-2", x = y, rjd3tramoseats::spec_tramoseats())
save_workspace(jws, file.path(dir, "workspace.xml"))
}
